package handlers

import (
	"fmt"

	"github.com/gofiber/fiber/v2"
)

// UpdateCircleHandle handle create a new circle
func UpdateCircleHandle(c *fiber.Ctx) error {
	fmt.Println("inside update circle handle")
	// Create the model object
	return nil
}
